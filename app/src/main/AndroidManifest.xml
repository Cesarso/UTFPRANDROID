<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher_confeitaria"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_confeitaria_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ControleDeVendasDeUmConfeiteiroAutonomo">

        <activity
            android:name=".ListagemProdutosActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ProdutosActivity"
            android:label="Cadastro de Produto"
            android:exported="false" />

        <activity
            android:name=".SobreActivity"
            android:label="Sobre o Autor"
            android:exported="false" />

    </application>

</manifest>